name: Deploy ebay-agent

on:
  workflow_dispatch:
    inputs:
      message:
        description: "Deploy reason (optional)"
        required: false
        default: "manual deploy"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /opt/apps/claude-workspace
            git fetch origin master
            git reset --hard origin/master
            echo "=== git log ==="
            git log --oneline -3
            echo "=== overview.html check ==="
            grep -c "ac blue\|top-row\|achievement-card" products/ebay-agent/templates/pages/overview.html || true
            cd products/ebay-agent
            docker compose down
            docker compose build --no-cache
            docker compose up -d
            echo "=== container status ==="
            docker compose ps
            echo "Deploy done: ${{ github.event.inputs.message }}"
