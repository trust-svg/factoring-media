# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=323107833

# GSC OAuth (file paths inside container)
GSC_CREDENTIALS_PATH=/app/credentials/gsc_oauth.json
GSC_TOKEN_PATH=/app/credentials/gsc_token.json

# Sites
# GSC property accepts URL prefix (https://example.com/) or sc-domain:example.com
# URL prefix版 = HTTPSのみ・サブドメイン除外、DNS認証不要（HTMLタグ等で確認済みならそのまま使える）
# sc-domain版 = 全サブドメイン横断、DNS TXT認証が必要
FACCEL_GSC_PROPERTY=https://faccel.jp/
FACCEL_DOMAIN=faccel.jp
FACCEL_CONTENT_DIR=/host/factoring-media/content/articles

SAIMU_GSC_PROPERTY=https://saimu-times.com/
SAIMU_DOMAIN=saimu-times.com
SAIMU_CONTENT_DIR=/host/saimu-media/site/content

# Storage
REPORTS_DIR=/app/reports
DB_PATH=/app/data/seo.db

# Analysis tuning
# 3-tier 分類: rewrite=5〜30 / title=30〜60 / seed=60〜100
# 立ち上げ期サイトは POSITION_MAX=100 推奨（種まきKWも可視化）
# 成熟サイトは POSITION_MAX=30 に絞ると 即リライト候補のみ抽出
MIN_IMPRESSIONS=10
POSITION_MIN=5.0
POSITION_MAX=100.0
LOOKBACK_DAYS=28
