// =============================================================
// ヤフオク入札フローのセレクタ定義
//
// P0 検証 (設計 §13) の状況。**検証済みと未検証が混在している**ので、
// 触るときは必ずこの表を更新すること。
//
// | セレクタ | 状態 | 根拠 |
// |---|---|---|
// | loginLink              | ✅ 検証済 | 2026-08-24/25。ログイン中0件 / 未ログイン2件(陰陽対照済) |
// | loggedInIndicator      | 🟡 旧UI限定 | 2026-08-25。旧UIヘッダのあるページでのみ当たる(下記5) |
//
// ⚠️ loginLink は入札フローだけでなく **セッションの生存確認**
// (jobs/verifySession.ts)でも使う。ここが外れると、失効を検知できないまま
// 入札の瞬間まで気づけない。判定の非対称性は src/sessionVerdict.ts を読むこと。
//
// | bidButton              | 🟡 文言は2つとも実測・照合は未検証 | 「入札する」= 2026-09-02 23:13 の実ページで2件・可視・`<button>`。「値段を上げて入札」= 2026-09-04 の実入札(d1242748141)の計測で `<button> 値段を上げて入札`。**この2つを1本の正規表現で拾う形は実ページで当てていない**(下記15) |
// | priceInput             | ✅ 検証済 | 2026-08-28 実測。`#inputPrice`(type=tel・**name無し**。下記11) |
// | bidConfirmButton       | ✅ 検証済 | 2026-08-29 の実入札(Stage 3)で押した先が確認画面だった(表示は「確認する」) |
// | bidSubmitButton        | ✅ 検証済 | 2026-08-29 実測。実確認画面でヒット1件・可視・「上記に同意のうえ 入札する」 |
// | wonIndicator           | ❌ 未検証 | 2026-08-29 実測で **終了後の商品ページには出ない**ことが判明。判定は bidHistory.ts へ移した |
// | highestBidderIndicator | ✅ 検証済(生きている商品ページ) | 2026-08-29 実測。`<h1>あなたが最高額入札者です</h1>`(旧値は0件だった)。2026-09-04 から **入札前のガード** にも使う(下記15) |
// | outbidIndicator        | ❌ 未検証 | 同上。負けた実例(n1242036522)でも商品ページには出なかった |
// | bidHistoryRow          | ✅ 検証済 | 2026-08-29 実測。入札履歴ページの `tr`(全5件・class/id 無し) |
// | watchlistLoginWall     | ✅ 検証済 | 2026-08-29 実測。判定の主役は URL 側。DOM は `input[name=handle]` だけが実在 |
// | watchlistItemLink      | ✅ 要スコープ | セレクタ単体では不合格。カルーセル除外と **必ずセット** で使う(地雷9) |
// | watchlistNextPage      | ❓ 判定不能 | 全滅したが、実物が9件でページャが出る状況ではない(地雷10) |
//
// ⚠️ 2026-08-25 に取ったウォッチリスト3件の「❌ 全滅」は **証拠として無効**。
//    叩いた URL が存在せず、404案内ページを読んでいた(下の地雷8)。
//
// ✅ 2026-08-26 実測: ウォッチリストの URL は
//    `https://auctions.yahoo.co.jp/my/watchlist`(トップページの「ウォッチ」
//    リンクの href そのもの)。ここでは loggedInIndicator が1件当たり、
//    loginLink が0件だった(ログイン済みの陰性側)。
//
//
// --- 実測で分かった地雷 (2026-09-02) ---
//
// 14. **商品URLはリダイレクトされる**。予約に保存しているのは
//     `https://page.auctions.yahoo.co.jp/jp/auction/<id>`(normalizeAuctionUrl)
//     だが、開いた先は `https://auctions.yahoo.co.jp/jp/auction/<id>`。
//     つまり `page.url() === auctionUrl` は **成立しない**。
//     「同じURLなら読み直しを省く」型の最適化はどこでも効かないし、
//     逆に「URLが変わった＝画面が遷移した」も成り立たない(地雷12: 入札の
//     3画面はすべて同じURL)。**URLの一致・不一致を画面の判断材料にしない**。
//     ⚠️ これを踏んだ実例: placeBid は `page.url() !== auctionUrl` で
//     読み直しを判定していたので、条件が **常に真** になり、monitor が
//     ウォームアップで描画まで待ったページを入札の瞬間に毎回捨てていた。
//     2026-09-03 修正。今は「商品IDが URL に含まれるか」＋「描画されて
//     いるか」の2点で見る(URL の一致は使わない)。
//
// UI変更時はこのファイルだけ直せば済むよう、Playwright操作側には
// セレクタを直書きしないこと。
//
// ⚠️ bidConfirmButton が ✅ になっても **P0 プローブの確定前ガードは外さない**。
// Stage 2 は実額を入力した状態でこのセレクタが掴んだ要素を押すので、これが
// 確定ボタンに当たっていた場合は本物の入札が飛ぶ(取り消せない)。
// 「2026-08-29 時点ではラベルが『確認する』だった」ことと「次に開いた時もそうだ」
// は別で、確認画面と商品ページは同一 URL・裏の入札ボタンも DOM に残る(地雷12)。
// ラベル一致だけで「確定ボタンではない」とは言えない。押す前の判定は
// src/bidder/probeSafety.ts。
//
// --- 実測で分かった地雷 (2026-08-24) ---
//
// 1. `gv-Button--yimO_UuGzSZbwqISHVu4` のような `gv-` 系クラスの末尾はビルドハッシュ。
//    Yahoo 側のデプロイで変わるので **クラス名に依存しない**。
// 2. `a[href*='/jp/show/bid']` は入札履歴リンク `/jp/show/bid_hist` に前方一致する。
//    入札ボタンだと思って使うと、入札の瞬間に履歴ページへ飛ぶ。
// 3. 入札ボタンは `<a>` ではなく `<button>`。id / name / href が無く、
//    掴めるのは**アクセシブルネーム(表示テキスト)だけ**。
// 4. 「入札する」ボタンの個数は実行によって変わる(ログイン中1件 / 未ログイン2件。
//    画面下の固定バー分と思われる)。件数を前提にした判定を書かない。
//
// --- 実測で分かった地雷 (2026-08-25) ---
//
// 5. 新UIはクライアントサイドレンダリング(CSR)。`domcontentloaded` 直後の DOM は
//    ほぼ空で、そこを読むと **セレクタが正しくても「0件」** になる。
//    ⚠️ この「0件」は「セレクタが違う」ときと見分けがつかない。
//    プローブ側の描画待ち・描画判定は src/bidder/pageReady.ts。
//    旧UIヘッダ(mhdPc*)のあるページと新UI(gv-*)のページが混在しているので、
//    loggedInIndicator は **どのページで見たか** とセットでないと意味がない。
// 6. `role=button[name="入札する"]` の **引用符付きの時点で既に完全一致**
//    (かつ空白正規化される)。`[exact=true]` は role セレクタの属性として
//    存在せず、書くと構文エラーで **常に0件** になる。
//    「まとめて入札する」を弾く目的は引用符だけで達成できている。
// 7. 商品ページに `data-testid` は `initViewLogging-csr` 以外1つも無い。
//    `[data-testid="bid-confirm"]` 等は完全な作り話だったので消した。
//    プレースホルダは「実在しそうな形」で置くこと。実在しない属性は
//    永久に0件のまま「まだ検証していないだけ」に見え続ける。
//
// --- 実測で分かった地雷 (2026-08-26) ---
//
// 8. **存在しない URL のヤフオク案内ページは「空のページ」に見えない**。
//    「指定されたURLのページは存在しません。」の案内にはヘッダー・
//    「カテゴリから探す」の30リンク・フッターが付くので、クリック要素が
//    50個超ある。つまり地雷5の描画判定(閾値5)を素通りし、
//    「描画完了・ログイン壁なし・商品0件」= **空のウォッチリストと
//    完全に同じ姿** になる。しかも404は誰にでも404なので、
//    `--anonymous` でログイン画面に飛ばないことまで説明が付いてしまい、
//    最大の手掛かりが潰れる。
//    ⚠️ セレクタの当たり外れを論じる前に、**着いたページが何なのか** を
//    先に確定させること。判定は src/bidder/pageIdentity.ts。
// 9. **ウォッチリストのページには「おすすめ」カルーセルが同居している**。
//    `a[href*="/jp/auction/"]` はウォッチ中の商品とカルーセルの商品の
//    両方に当たる。カルーセルは遅延読み込みなので、同じページを20秒差で
//    2回読むと 128件 → 142件(商品IDの重複を除いて 64件 → 71件)になった。
//    ⚠️ この誤りは0件と違って **正常な顔で通る**。件数が多いので
//    「ちゃんと取れている」ようにすら見え、scrapeWatchlistPage は OK を返し、
//    ウォッチしていない商品が予約候補に並ぶ。
//    ✅ 2026-08-27 に内訳を実測して確定した(プローブの ancestryReport):
//      65商品 × 2本 = 130本 … `gv-Carousel__item < gv-Carousel__items
//                              < gv-Carousel__body < gv-Carousel` の中
//       9商品 × 2本 =  18本 … Carousel を通らない `gv-Stack < ... < gv-Card`
//      計148本・商品ID 70件。**ウォッチ中は9件だけ**。ページの checkbox も
//      10個(9行 + 全選択)で9件を裏付ける。
//    → 除外は `[class*="gv-Carousel"]` を祖先に持つものを落とす形で行う
//      (src/bidder/watchlistScope.ts)。クラス名には `--<ハッシュ>` が付くので
//      完全一致のクラスは存在しない。前方一致で見ること。
//    ⚠️ **「2回読んで件数が一致」では捕まらない**。同日の実測では汚染された
//      まま 70件 → 70件 で一致した。描画が落ち着けばカルーセルも安定する。
//      listStabilityVerdict は不合格を出すための検査であって、合格の根拠ではない。
//    ⚠️ 同期ジョブは 2026-08-27 まで描画完了を待っていなかった(プローブにしか
//      待つ処理が無かった)。そのため同じページを回ごとに9〜33件と別々に見て、
//      DB に毎時おすすめ商品が積もり 251件まで膨らんだ(実物9件)。
//      待ち方の実体は src/bidder/settle.ts に一本化した。写しを作らないこと。
// 10. **`/my/watchlist` に「次へ」が無いのは、ページャが無い証拠にならない**。
//    2026-08-27 時点で実物が9件しか無く、そもそもページャが出る状況ではない。
//    ⚠️ ここで watchlistNextPage を「存在しない」と結論すると、ウォッチが
//    増えた日に2ページ目を黙って取りこぼす(0件ではなく **1ページ目だけ**
//    返るので、成功の顔で欠落する)。ページャの有無は、ウォッチが十分に
//    増えてから、または「終了分」タブで件数が多い状態で確かめること。
//
// --- 実測で分かった地雷 (2026-08-28 / Stage 2) ---
//
// 11. **入札フォームはモーダル。URL が変わらない**。「入札する」を押しても
//    到達URLは商品ページのまま(実測: クリック前後とも
//    `/jp/auction/j1241905417`)。増えるのは input が11→12個、
//    クリック要素が223→225個だけ。
//    ⚠️ ここから3つ、別々の結論が出る:
//      a) 入札額の入力欄は `input#inputPrice`(type=tel)。
//         **name 属性が無い**ので `input[name=...]` 系は全部永久に0件。
//         実際、旧プレースホルダ `input[name="Bid_price"], input[name="price"]`
//         は当たらなかった(地雷7と同じ「実在しない属性」だった)。
//         ⚠️ 代わりに `input[type="text"]` で拾おうとしないこと。
//         ページ上部のヘッダ検索窓(`#mhdSearchBoxInput`)に当たる。
//      b) 確認ボタンの表示は **「確認する」**。「入札内容を確認する」ではない。
//      c) URL が変わらない以上、placeBid.ts の `navigated` は **常に false**。
//         bidSubmitButton が bidButton と同じ文字列のままだと、
//         モーダルの裏に残っている商品ページの「入札する」を掴んで
//         sameAsBidButton=true になり、確定は必ず中止される。
//         これは安全側の停止だが、**このままでは入札は一度も完了しない**。
//         確認画面の確定ボタンの実体を取るまで worker を本番稼働させないこと。
//      d) モーダルが開いている間も、裏の商品ページの「入札する」ボタン2件は
//         DOM に残る。つまり **確認画面に着いていないのに**
//         `role=button[name="入札する"]` は2件当たる。
//         2026-08-28 の Stage 2 レポートは、この状態の bidSubmitButton を
//         「✅ 検証済」として出した(偽陽性)。c で書いた
//         「裏の入札するを掴む」が実際に起きることの実測でもある。
//         → プローブは確認ボタンを押した後にしか bidSubmitButton を見ない
//           (scripts/p0-probe.ts の STAGE2_SLOTS)。
//         → この欄が ✅ に見えても、根拠が「確認ボタンを押した後の計測」
//           でない限り信用しないこと。
// 12. 確認画面(2026-08-28 実測・Stage 2 完走)
//     ⚠️ 確認画面も **モーダルで URL が変わらない**。商品ページ → 入札フォーム →
//     確認画面の3画面すべてが同じ URL。
//      a) 確定ボタンの表示は
//         **「上記のガイドライン等、情報提供に同意して 入札する」**。
//         実体は id も name も無い <button class="gv-Button--... gv-Button--primary">。
//      b) 確認画面でも `role=button[name="入札する"]` は **2件** 当たる。
//         これは裏の商品ページのボタン2件で、確定ボタンではない
//         (a の完全一致に外れるので role セレクタからは見えない)。
//         `button:has-text('入札する')` は 3件(裏2件 + 本物1件)。
//         `input[type='submit'][value*='入札']` は 0件。
//      c) **確認画面に着いた positive な証拠は `#inputPrice` の消滅だけ**
//         (input 12個 → 11個)。URL も裏のボタンも変わらないので、
//         他に「進んだ」と言える材料が無い。placeBid.ts はこれを
//         formStillOpen として submitTargetVerdict に渡している。
//      d) 所要時間: 入札ボタン 173ms / 入札額入力 19ms / 確認画面へ 125ms
//         = 合計 317ms(設計 §13-2 の実測値)。
//    ⚠️ さらに大事なこと: このとき出た報告は
//    「入札額の入力欄が1つも無い → 入札フォームに着いていない」だった。
//    実際には着いていて、**セレクタが違っただけ**。
//    着地判定は priceInput の正しさに乗っているので、
//    セレクタが外れると「着いていない」に化ける。文言に
//    「セレクタが違う」possibility を残してあるのはそのため(pageReady.ts)。
// =============================================================

// --- 実測で分かった地雷 (2026-09-02 / 初の本番入札) ---
//
// 13. **`role=button[name="入札する"]` が15秒待って0件になった実例がある**
//    (k1242598835・dryRun=false・終了330秒前・リトライも同じ Timeout)。
//    同じ回で価格取得(HTTP fetch)は成功して ¥16,000 を読めているので、
//    「ヤフオクに届いていない」わけではない。**ブラウザで見た DOM にだけ
//    入札ボタンが無かった**。
//    ⚠️ 残っていた記録は Timeout の1行だけで、次の3つを切り分けられなかった:
//      a) 文言か要素の種類が変わった(旧UIの入口は `<a href="/jp/show/bid">`
//         で、role=button のセレクタからは永久に見えない。地雷2/5)
//      b) CSR がマウントしておらず DOM が空だった(地雷5)
//      c) そもそも商品ページに居なかった(ログイン壁・エラー・別画面)
//    → 掴めなかった時点で画面を計測して残すようにした
//      (bidder/diagnose.ts の captureBidEntry。URL・タイトル・要素の種類・
//       「入札」を含む可視要素・クリック要素数を1行で残す)。
//    → 監視ジョブ側もウォームアップで描画を待つようにした(b の切り分け)。
//
//    --- 2026-09-02 23:13 の P0 プローブ(生きている商品 1242498080)で分かったこと ---
//
//    ✅ **a) は反証された**。実ページで `role=button[name="入札する"]` は
//       **2件・可視・`<button>`**。旧UIの入口とされる
//       `a[href*='/jp/show/bid']:not([href*='bid_hist'])` は **0件**で、
//       当たった `a[href*='/jp/show/bid']` は入札履歴(罠・地雷2)だけだった。
//       → **旧UI入口をフォールバック候補として足さないこと**。実ページに存在しない。
//       `#bid` も `form[name='bidform'] input[type='submit']` も 0件のまま。
//    ❌ **「リトライが1回目と同じ DOM を触った」という説明は誤りだった**。
//       予約に保存している URL は `page.auctions.yahoo.co.jp/...` だが、
//       実ページは `auctions.yahoo.co.jp/...` に **リダイレクトされる**(下記14)。
//       つまり `page.url() !== auctionUrl` は常に真で、1回目も2回目も
//       読み直したうえで15秒0件だった。**新しく開いた画面で2回とも無かった**。
//    → 残る筋は b) c) と、プローブでは踏めなかった次の2つ:
//       d) headless / コンテナ固有の差(プローブは headless:false・Mac 上、
//          ワーカーは headless:true・Docker。session.ts)
//       e) その商品だけ入札できない状態だった(入札者制限・本人確認要求など。
//          このときページには「入札」を含む別の文言が出るので、
//          captureBidEntry の可視要素一覧に出るはず)
//    ✅ **d) も反証された**。2026-09-03 08:54 に `--headless` で測っても
//       同じく 2件・可視(描画 1284ms / クリック要素 229)。headless そのものは
//       効いていない。残る差は **ワーカーの実行環境(Docker コンテナ・
//       イメージ同梱の Chromium・session.ts の起動オプション)** か e)。
//    ✅ **コンテナ差も反証された**。2026-09-03、本番と同じ session.ts の
//       経路で `docker compose exec worker npx tsx
//       apps/worker/scripts/bid-entry-check.ts <URL>` を実行(読むだけ・
//       クリックしない)。入札ボタン **2件**・ログインリンク0件・判定 OK。
//       → **環境は無罪**。a) c) d) は全部落ちた。
//    ⚠️ ただしこの実行で別の数字が出た: **描画待ちがコンテナで 7,241ms**
//       (同じ商品ページで Mac は 1,284〜1,516ms。無負荷で約5倍)。
//       これが効く理由が placeBid 側にあった:
//         - monitor はウォームアップで描画まで待っている
//         - なのに placeBid が **無条件で開き直していた**(URL 一致で
//           判定していたが、下記14のリダイレクトで条件が常に真)
//         - 開き直した DOM は CSR でほぼ空。その後の15秒のセレクタ待ちが
//           **マウント時間ごと** 背負う = 予算の半分を描画が食う
//       → 2026-09-03 修正: 同じ商品ページで描画済みなら開き直さない /
//         開き直したときは描画を待つ(待ち時間は残り時間で上限を切る。
//         snipeSecondsBefore は最短5秒なので固定15秒は追い越す)。
//       → b) が最有力になった。次の失敗は detail に `[描画待ち]` の実測が
//         残るので、0件だったのか描き終わっていなかったのかが分かる。
//    ✅ 計測そのものは実ページで動作確認済み(2026-09-03 のプローブに
//       captureBidEntry の出力を載せた。「入札」を含む可視要素は
//       `<button> 入札する` と出る)。失敗時に初めて動かすわけではない。
//
//    ⚠️ **この欄を ✅ に戻すのは、0件になった条件が説明できてから**。
//    文言が今日当たったことは「入札の瞬間にも当たる」を意味しない。

// --- 実測で分かった地雷 (2026-09-04 / 2件目の実入札失敗) ---
//
// 15. **入札の入口ボタンの文言は「入札する」だけではない。自分がその商品に
//     入札済みだと「値段を上げて入札」に変わる**。
//     実測(d1242748141・上限¥33,500・終了330秒前・1回目とリトライの両方が Timeout):
//       現行セレクタのヒット=0件 / ログインリンク=0件 / クリック要素=235個 /
//       「入札」を含む可視要素="<button> 値段を上げて入札"
//       [描画待ち] 1337ms / クリック要素235個 / 判定 OK
//     ⚠️ ここで **地雷13の b)(CSR未マウント)は反証された**。描画は 1.3秒で
//     完了していて要素は235個ある。0件だったのは文言違いであって、
//     待ち時間の問題ではなかった。13の修正(温めたページを捨てない/描画を待つ)
//     は正しく効いていて、1回目は goto せず温めたページを使えている。
//
//     ✅ **「入札があるから文言が変わる」ではない**(対照実験で反証済み)。
//     同日、他人の入札が入っている生きている商品(e1236135848・現在価格¥27,800)を
//     同じ経路で読むと `role=button[name="入札する"]` が **2件**・
//     「入札」を含む可視要素は `<button> 入札する` だった。
//     変わる条件は商品側ではなく **自分がその商品に入札済みかどうか**。
//
//     ⚠️ そして「値段を上げて入札」は **最高額入札者のときにも、上回られて
//     いるときにも出る**。d1242748141 は手動入札の後に他人へ価格更新された
//     状態だった(本人確認)。つまり:
//       - この文言を見て「入札しない」に倒すと、**競り上げという予約の目的
//         そのもの** が動かなくなる。文言では止めないこと。
//       - 止めてよいのは自分が最高額入札者のときだけで、その判定は別物
//         (highestBidderIndicator の h1)。placeBid はそちらで判定する。
//
//     ⚠️ 確認画面の確定ボタンの文言が、この商品でどうなるかは **未実測**。
//     入口が「値段を上げて入札」なら確定側も「…同意…値段を上げて入札」に
//     なりうるので、bidSubmitButton の正規表現は「入札する」ではなく
//     「入札」までに緩めてある(判別に効いているのは「同意」の方)。
//     外れていた場合は4点ガードが止め、captureConfirmScreen が実ラベルを残す。

export const selectors = {
  // --- ログイン状態の判定 ---
  //
  // 判定の主役は **loginLink の不在**。実測でログイン中0件 / 未ログイン2件と
  // きれいに分かれた唯一の指標で、陰性対照(--anonymous)まで取れている。
  // loggedInIndicator は補助。ログイン中の1回でしか確認できていないので、
  // 「これが無い = 未ログイン」という判定には使わないこと(誤検知で入札を捨てる)。
  loggedInIndicator: "a.mhdPcUserName__link",
  loginLink: 'a[href*="login.yahoo.co.jp"]',

  // --- 商品ページ → 入札フォーム ---
  //
  // role セレクタを使うのは、実体が <button> で id も name も無く、
  // クラスがビルドハッシュ付きだから。
  //
  // ⚠️ 文言は1つではない。**自分がその商品に入札済みだと「値段を上げて入札」**
  // に変わる(地雷15)。`name="入札する"` の完全一致だけにしていた間、
  // 入札済みの商品では永久に0件で、2026-09-04 の予約は15秒×2回とも
  // Timeout で終わった。
  //
  // ⚠️ 正規表現に `^` `$` を付けたまま増やすこと。囲まないと
  // 「まとめて入札する」に当たる(引用符の完全一致でそれを弾いていたのが
  // 元の形。地雷6)。ここが緩むと、まとめて入札の画面に入り込む。
  // ⚠️ 「入札」だけに縮めないこと。入札履歴リンク・「入札を取り消す」など
  // 別物まで拾う(地雷2と同じ形の罠)。
  bidButton: "role=button[name=/^(入札する|値段を上げて入札)$/]",

  // ✅ 2026-08-28 実測。「入札する」を押すと **モーダル** が開く(遷移しない)。
  // 入力欄は id だけが手掛かり(name 属性が無い。地雷11a)。
  // type=text で広く拾うとヘッダの検索窓に当たるので、id を外さないこと。
  priceInput: "input#inputPrice",
  // ✅ 2026-08-28 実測。表示は「確認する」(「入札内容を確認する」ではない)。
  // ⚠️ 押した先(確認画面)はまだ見ていない。
  bidConfirmButton: 'role=button[name="確認する"]',
  // 確認画面 → 入札確定。ここを押すと **取り消せない**。
  // 罠を掴んだときの被害が最大なので、広い候補を足さないこと。
  //
  // 🟡 確定ボタンの実ラベルは **商品によって変わる**。実測できているのは2つ:
  //   2026-08-28 21:00 「上記のガイドライン等、情報提供に同意して 入札する」(Stage 2)
  //   2026-08-28 22:44 「上記に同意のうえ入札する」(DRY_RUN・[確認画面の実測])
  //   2026-08-29 00:35 「上記に同意のうえ 入札する」(Stage 2・実画面でヒット1件)
  // 最初は前者だけを見て `has-text('情報提供に同意して')` にしていたが、
  // 後者の商品ではヒット0件で入札が成立しなかった。**1商品で見た文言を
  // そのままセレクタにしない**。共通しているのは「同意」と「入札する」だけ。
  //
  // ⚠️ `role=button[name="入札する"]` に戻さないこと。確認画面でも
  // 裏の商品ページのボタン2件に当たり、押しても入札は成立しないのに
  // 成功扱いになる(地雷12b)。完全一致の role セレクタからは本物が見えない。
  // 「同意」が要るのは、裏のボタンが**ちょうど**「入札する」だから。
  //
  // ⚠️ `button:has-text('入札する')` に戻さないこと。2026-08-29 の実確認画面で
  // **3件**(裏の商品ページの「入札する」2件 + 本物)。1件目を押しても入札は
  // 成立しないのに成功扱いになる。
  // `<input type="submit">` の可能性は実測で消えた(確定ボタンは `<button>`、
  // `input[type='submit'][value*='入札']` は0件)。それでも role セレクタの
  // ままにしてあるのは、UI が入れ替わったとき has-text だけが静かに0件へ
  // 落ちるから。3形とも当たることは tests/browser/submitSelector.test.ts で担保。
  //
  // ⚠️ URL は確認画面でも変わらない(地雷12)。「遷移したから別画面」という
  // 判断材料は最後まで無い。押してよいかの判定は
  // src/bidder/probeSafety.ts の submitTargetVerdict に集約してある
  // (見つかる / #inputPrice が消えている / ラベルが商品ページ側でない /
  //  最初に押した入札ボタンと別要素、の4点を全部要求する)。
  // ⚠️ 2026-09-04: 末尾を「入札する」から「入札」に緩めた。入口の文言が
  // 商品ページ側で「値段を上げて入札」に変わる以上(地雷15)、確認画面の
  // 確定ボタンも「…同意…値段を上げて入札」になりうる。実ラベルは未実測で、
  // 狭いまま当てに行くと **入札済みの商品では確定に一度も届かない**。
  // 判別に効いているのは「同意」の方で、裏に残る商品ページのボタン
  // (「入札する」「値段を上げて入札」ちょうど)には「同意」が無い。
  bidSubmitButton: "role=button[name=/同意[\\s\\S]*入札/]",

  // --- 結果判定(終了後) ---
  //
  // ⚠️ 勝敗は **商品ページでは判定できない**。2026-08-29 実測(n1242036522):
  // 終了後の商品ページの状態表示は「このオークションは終了しています」だけで、
  // wonIndicator / outbidIndicator の候補は全滅した(落札者名すら出ない)。
  // 実際の判定は入札履歴ページを読む bidHistory.ts に移した。
  //
  // ❌ 下の2つは **実文言が確認できていない**。商品ページに出ないことは
  // 分かったが、「どこかに別の文言で出る」可能性を否定できていないので
  // 消さずに残す。ただし checkResult はもうこれを見ていない。
  wonIndicator: "text=あなたが落札しました",
  // ✅ 2026-08-29 実測(Stage 3 の読み戻し)。実文言は `<h1>あなたが最高額入札者です</h1>`。
  //
  // ⚠️ 直前まで置いていた「あなたが**現在の**最高額入札者です」は **0件** だった。
  // 実在しない文言を書くと、そのセレクタは永久に0件のまま
  // 「まだその状態になっていないだけ」に見え続ける(地雷7と同じ形)。
  //
  // ⚠️ `text=最高額入札者` まで短くしないこと。Playwright の `text=` は部分一致
  // なので、否定形(「最高額入札者ではありません」等)にも当たりうる。
  // 「入札できていないのに成立と読む」側に倒れる誤りなので、
  // 述語の非対称性を潰すために「者です」まで含めた形を正とする。
  //
  // ⚠️ 2026-09-04 から、これは結果判定だけでなく **入札を実行するかどうか**
  // も決める(placeBid の ALREADY_HIGHEST)。誤検知の代償が
  // 「入札しないまま終わる」に変わったので、部分一致で広げないこと。
  // 出典は生きている商品ページ(2026-08-29 Stage 3 の読み戻し)で、
  // 終了後のページではない。
  highestBidderIndicator: "text=あなたが最高額入札者です",
  outbidIndicator: "text=高値更新",

  // 入札履歴ページ(/jp/show/bid_hist?aID=...)の行。
  // ✅ 2026-08-29 実測。この表には class も id も無く(素の table が3つ)、
  // ページ全体で `tr` は5件しかない。うち3件が見出し・案内で、
  // 残り2件が入札者の行だった。**行の絞り込みはセレクタではなく
  // テキスト(「評価」を含むか / 自分の表示名を含むか)で行う**
  // — 詳細と判定は bidHistory.ts。
  bidHistoryRow: "tr",

  // --- ウォッチリスト(ログイン必須) ---
  //
  // ウォッチリストは未ログインだと商品が1件も出ず、ログイン画面へ飛ばされる。
  // つまり「0件」と「ログインが切れている」が **同じ見た目**になる。
  // watchlistLoginWall を先に見て、空リストと取得失敗を必ず区別すること
  // (区別しないと、セッション切れが「ウォッチリストが空になった」に化ける)。
  //
  // ⚠️ ログイン壁の判定の主役は **このセレクタではなく URL**
  // (pageIdentity.ts が login.yahoo.co.jp への遷移を見ている)。
  // 2026-08-29 実測: 未ログインで /my/watchlist を開くと
  //   https://login.yahoo.co.jp/config/login?.src=auc&.done=... へ **302 されて HTTP 200**。
  // つまりステータスは正常系のまま、DOM もヤフオクのものではなくなる。
  // ここは「遷移せず同じページ内に壁が出た」場合の後詰め。
  //
  // ⚠️ 直前まで置いていた `form[action*="login.yahoo.co.jp"], input[name="login"]`
  // は実在せず、**本物のログイン壁に対して0件**だった(同時に試した
  // `form[action*=...]` 単体・`input[name='login']`・`text=ログインしてください`
  // も全て0件)。書いた本人には二重化したつもりの行が、実際には一度も
  // 効かないまま「壁を見る処理はある」という安心だけを作っていた。
  // 実測で存在したのはログインID入力欄の `handle` だけ(id は毎回変わる乱数、
  // data-testid は1つも無い)。
  //
  // ⚠️ `text=ログイン` に広げないこと。同じログイン画面で3件当たり、
  // うち1件は「パスワードのみでの**ログイン**を終了します」という別物だった
  // (Playwright の `text=` は部分一致)。
  watchlistLoginWall: 'input[name="handle"]',
  // 1商品ぶんのリンク。オークションIDを含む href を手掛かりにする
  watchlistItemLink: 'a[href*="/jp/auction/"]',
  // ページャの「次へ」。無ければ1ページで打ち切る
  // `[exact=true]` は不正だったので外した(上の地雷6)
  watchlistNextPage: 'role=link[name="次へ"]',
} as const;
